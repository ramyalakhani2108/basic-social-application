<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-between mb-5 border-b border-white">
            <h1 class="text-3xl pb-5 "><span class="font-light">Edit Your Post</span>....</h1>
            <form action="/logout" method="post">
                <button type="submit" class="bg-transparent pt-2 text-red-400 font-bold">Logout</button>
            </form>
        </div>
        
        <h5 class="mb-5">Here you can create your posts.</h5>

        <form action="/update/post" method="post">
            <textarea class=" mb-5 w-1/3 resize-none block bg-transparent border-2 border-zinc-700 rounded-md outline-none p-3 " name="content" placeholder=" what's in your mind 🤯 ?"><%= post.content.trim() %></textarea>
            <input type="hidden" name="id" value="<%= post._id %>">
            <input type="submit" class="px-3 w-40 text-sm py-2 bg-yellow-900 rounded-lg block" value="update">
        </form>
    </div>
</body>

</html>